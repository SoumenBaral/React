import React from 'react';

const DeviceDetails = ({price}) => {
    return (
        <div>
            <h5>The price of the Device is : {price}</h5>
        </div>
    );
};

export default DeviceDetails;